{% extends 'base.html.twig' %}

{% block title %}Editar una cançò{% endblock %}

{% block body %}
    <div class="container d-flex justify-content-center py-5">
        <div class="row">
            <div class="col-12">
                <h2 class="fs-5 border-3 border-bottom border-info-subtle">Editant una cançò</h2>
            </div>
        </div>
    </div>

    {{ include('song/_form.html.twig', {'button_label': 'Modificar'}) }}

    <!--<a href="{{ path('app_song_index') }}">back to list</a>-->

    <!--{{ include('song/_delete_form.html.twig') }}-->
{% endblock %}
