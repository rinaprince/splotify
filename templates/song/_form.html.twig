{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}
<div class="container bg-white">
    <div class="row">
        <div class="col-3">
            {{ form_row(form.title, {'label': 'Titol', 'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="col-3">
            {{ form_row(form.duration, {'label': 'Duració:','attr': {'class': 'form-control'}}) }}
        </div>
        <div class="col-3">
            {{ form_row(form.album, {'label': 'Album', 'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
</div>
{{ form_widget(form) }}
<div class="container py-5">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between">
                <button class="btn btn-danger"><a href="{{ path('app_song_index') }}"
                                                            class="link-underline link-underline-opacity-0 text-white">Torna
                        enrere</a></button>
                <button class="btn btn-outline-success">{{ button_label|default('Guardar') }}</button>
            </div>
        </div>
    </div>
</div>
{{ form_end(form) }}
