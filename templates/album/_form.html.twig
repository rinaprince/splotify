{{ form_start(form) }}
<div class="container bg-white">
    <div class="row">
        <div class="col-6 mb-5">
            {{ form_row(form.title, {'label': 'Titol', 'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="col-6">
            {{ form_row(form.releasedAt, {'label': 'Data de llançament:','attr': {'class': 'form-control','type': 'date', 'value': form.vars.value.releasedAt|date('Y-m-d')}}) }}
        </div>
        <div class="col-6 mb-5">
            {{ form_row(form.band, {'label': 'Banda', 'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="col-6">
            {{ form_row(form.coverFile, {'label': 'Portada', 'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
</div>
{{ form_widget(form) }}
<button class="btn btn-outline-success ms-5 mt-5">{{ button_label|default('Guardar') }}</button>
{{ form_end(form) }}
